import { createMuiTheme } from '@material-ui/core'

export const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#20B2AA',
      contrastText: 'white',
    },
    secondary: {
      main: '#FF69B4',
      contrastText: 'white',
    },
  },
})

export default theme
